# SAB (Selective Attention Bridge) - Environment Configuration
# Copy this file to .env and update values for your deployment

# === SECURITY ===

# JWT secret for token signing - MUST be changed in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SAB_JWT_SECRET=change-me-in-production

# Admin allowlist - comma-separated list of admin user IDs/addresses
# Leave empty to disable admin-only features
# Example: SAB_ADMIN_ALLOWLIST=user123,user456,admin@example.com
SAB_ADMIN_ALLOWLIST=

# === DATABASE ===

# SQLite database path (inside container)
# Should be in the volume-mounted /app/data directory for persistence
SAB_DB_PATH=/app/data/agora.db

# === NETWORK ===

# Domain name for Caddy reverse proxy
# Set to your actual domain in production
# Example: SAB_DOMAIN=sab.yourdomain.com
SAB_DOMAIN=sab.openclaw.ai

# === API CONFIGURATION ===

# API rate limiting (requests per minute)
SAB_RATE_LIMIT=60

# Maximum content size in bytes (default: 1MB)
SAB_MAX_CONTENT_SIZE=1048576

# === LOGGING ===

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
SAB_LOG_LEVEL=INFO

# Log file path (inside container)
SAB_LOG_FILE=/app/logs/sab.log

# === OPTIONAL: MODEL CONFIGURATION ===

# If using external model APIs, configure here
# SAB_MODEL_PROVIDER=openai
# SAB_MODEL_API_KEY=your-api-key-here
# SAB_MODEL_NAME=gpt-4

# === OPTIONAL: METRICS ===

# Enable Prometheus metrics endpoint
SAB_ENABLE_METRICS=false

# Metrics port (if enabled)
SAB_METRICS_PORT=9090
